
@model ClientPortal.Models.QuestionModel

@{
    Html.RenderPartial("_QuestionComponent", Model);
}

@section scripts {
    <script type="text/javascript">

        $(document).ready(function () {

            setQuestionPosition('MedicAlert');

            function toggleSubmit() {
                var canSubmit = $(`input[name$='HasMedicAlert']:checked`).val() === 'false' || $(`input[name$='HasMedicAlert']:checked`).val() === 'true';

                toggleSubmitBtn(canSubmit);
            }

            toggleSubmit();

            $(`input[name$='HasMedicAlert']`).click(function () {
                toggleSubmit()
            });

        });

    </script>
}
